# Status: raw-launch diagnostic

This bundle is not paper-equivalent for JavaScript Native. It uses raw
`native-launch` E2E for uploader, thumbnailer, and Alexa. The released artifact
instead constructs a fork-equivalent JavaScript Native baseline.

Use the corrected comparison at:

`/home/booklyn/cofunc-tdx/reports/measured_tdx_fig11_paper_aligned_20260717_060234`

The Python, CoFunc, and Vanilla Kata source measurements remain valid. This
status changes only the interpretation of the JavaScript Native bars.
