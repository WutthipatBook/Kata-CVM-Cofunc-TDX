--- a/cvm_os/kernel/arch/x86_64/boot/intel_tdx/iso/boot/grub/grub.cfg
+++ b/cvm_os/kernel/arch/x86_64/boot/intel_tdx/iso/boot/grub/grub.cfg
@@ -3,5 +3,10 @@ set default=0
 
 # Boot menu entries
 menuentry "IPADS ChCore x86-64" {
+	echo "[GRUB probe] before multiboot2"
 	multiboot2 /boot/kernel.img
+	echo "[GRUB probe] after multiboot2"
+	echo "[GRUB probe] before boot"
+	boot
+	echo "[GRUB probe] boot returned"
 }
